yt-dlp>=2024.1.0
notebooklm-py[browser]>=0.1.0

# Qualitative Research Toolkit (standard library only — no extra installs needed)
# Optional enhancements:
# pip install rich          # for coloured terminal output
# pip install pandas        # for tabular exports
# pip install openpyxl      # for Excel codebook export
